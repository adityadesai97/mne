VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# Optional: only allow emails present in public.allowed_emails
VITE_RESTRICT_SIGNUPS=false

# Optional: show the landing page before sign-in
VITE_LANDING_AS_HOME=false

# Optional (only needed when enabling push notifications)
# VITE_VAPID_PUBLIC_KEY=your-vapid-public-key
